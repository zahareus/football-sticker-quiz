# StickerHunt Architecture

## Overview

Global football sticker database with quiz, battle mode, and interactive map.
Static HTML pages generated by Node.js scripts, data in Supabase.

**Domain:** stickerhunt.club
**Repo:** https://github.com/zahareus/football-sticker-quiz/

## Stats
<!-- AUTO-UPDATED by generate-static-pages.js -->
- **Stickers:** 3539
- **Clubs:** 715
- **Countries:** 58
- **Cities:** 22+
<!-- /AUTO-UPDATED -->

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend | Static HTML/CSS/JS |
| Backend | Supabase (PostgreSQL + Storage + Auth + Edge Functions) |
| Hosting | GitHub Pages + Vercel (hybrid) |
| Automation | n8n webhooks + GitHub Actions |
| Analytics | Google Analytics (G-4Y0MJKGDZS), PostHog |

## Database (Supabase)

| Parameter | Value |
|-----------|-------|
| URL | `https://rbmeslzlbsolkxnvesqb.supabase.co` |
| Project ref | `rbmeslzlbsolkxnvesqb` |
| Storage bucket | `stickers` |

**Tables:** `stickers`, `clubs`, `countries`, `users`, `quiz_sessions`, `leaderboard`, `battles`, `sticker_stats`

**Note:** DDL (CREATE/ALTER TABLE) requires SQL Editor in Supabase Dashboard. REST API only allows DML.

## Page Generation Pipeline

### Upload Flow (new sticker)
```
User uploads via web form (upload.js)
  -> File stored in Supabase Storage (stickers bucket)
  -> Record inserted in stickers table
  -> EXIF data extracted (GPS, date), reverse geocoded
  -> n8n webhook triggered (async, non-blocking)
    -> Social media post scheduled (if enabled)
    -> GitHub Actions repository_dispatch triggered
      -> generate-single-sticker.js {id}
        -> stickers/{id}.html + clubs/{club_id}.html + countries/{CODE}.html
      -> optimize-images.js --only={id}
        -> {image}_web.webp (600x600, q80) + {image}_thumb.webp (150x150, q75)
      -> generate-city-pages.js
        -> cities/{slug}.html
      -> git commit + push -> deploy
```

### Batch Upload Flow (no social post)
```
upload-batch.html / upload-batch.js (separate page; upload.html stays as-is)
  -> drop N JPEGs -> N rows (thumb, club autocomplete, difficulty, EXIF geo)
     (incremental drops accumulate in one session; rows without a club are skipped)
  -> confirm() -> per row: Supabase Storage + stickers INSERT (NO per-sticker webhook)
  -> ONE POST to n8n "SH batch reconcile" (webhook /sticker-batch-uploaded, id kpeWoT8qqyq0Gdrq)
    -> ONE repository_dispatch {sticker_ids: "1,2,...", notify: true, n_stickers/n_clubs/n_countries}
      -> generate-sticker-pages.yml generates ALL pages in a single run + optimizes images
      -> final step: Telegram alert (Самаритянин, chat 292048) with counts; gated on notify=true
  -> page swaps to a report view listing every sticker sent for generation
```

### Club Creation & Enrichment
```
club-create.html / club-create.js
  -> insert clubs row {name, country}
  -> AI enrichment via /api/enrich-club (Vercel serverless, OPENAI_API_KEY):
       city ("City, Country"), media (5 hashtags), web (Wikipedia/official URL)
  -> update clubs row with enriched fields
  -> clubs poller (n8n, every minute) detects the change -> regenerates club/country pages

Re-enrich (added 2026-06-06): pick an EXISTING club from the autocomplete ->
  "Re-enrich this club" button -> re-runs /api/enrich-club -> updates the row
  (only fields that came back; never wipes with null) -> poller regenerates the page.
  Fixes one-off transient enrichment failures without re-creating the club.
```

### Generators

| Script | Creates | Trigger |
|--------|---------|---------|
| `generate-single-sticker.js` | sticker + club + country pages | Each upload (GitHub Actions) |
| `regenerate-club-pages.js` | club + country pages | Manual (mass regen) |
| `regenerate-country-pages.js` | all country pages | Manual |
| `regenerate-club-sticker-pages.js` | sticker pages for one club | Club rename |
| `regenerate-stickers-batch.js` | specific sticker pages by IDs | Manual fix |
| `regenerate-all-stickers.js` | ALL pages | Major update (rare) |
| `generate-city-pages.js` | city pages + index | Each upload |
| `generate-static-pages.js` | ALL pages (local full regen) | Manual |
| `optimize-images.js` | _web.webp + _thumb.webp | Each upload |

**IMPORTANT:** `generate-single-sticker.js` and `regenerate-club-pages.js` have their OWN copies of helper functions. When changing layout -- update BOTH files + `seo-helpers.js`.

### Shared Module: seo-helpers.js

All generators import from `scripts/seo-helpers.js`:
- Country names/flags/languages
- Image URL helpers (optimized, thumbnail, detail)
- Template loading and placeholder replacement
- Breadcrumbs generation
- Alt text generation (descriptive, multilingual)
- Paginated Supabase queries

### Templates

| Template | Pages | Key Features |
|----------|-------|-------------|
| sticker-page.html | Individual stickers | Club mini-card, nearby stickers, city map |
| club-page.html | Club pages (V2) | Wiki facts, sticker gallery, internal links |
| country-page.html | Country pages (V2) | Flag, top stickers strip, club cards grid |
| city-page.html | City pages | Wikipedia intro, Leaflet map, sticker gallery |
| index-page.html | Homepage | SOURCE OF TRUTH for index.html |

### Data Sources

| Source | Content |
|--------|---------|
| Supabase `stickers` | id, club_id, image_url, location, lat/lng, rating, games, difficulty |
| Supabase `clubs` | id, name, country, city, web, media |
| `wiki-cache.json` | 675 entries: Wikipedia (560) + AI-generated (114) |
| `city-wiki-cache.json` | City Wikipedia data (population, intro) |

### Image Pipeline

```
Original upload -> Supabase Storage: stickers/club_{id}/{timestamp}_{filename}.jpeg
                -> Edge Function (async): creates _web.webp + _thumb.webp
                -> GitHub Actions: optimize-images.js (backup)

URLs in HTML:
  OG image:  {base}_web.webp (top-rated by ELO)
  Gallery:   {base}_thumb.webp (150x150)
  Detail:    {base}_web.webp (600x600)
  Full size: original .jpeg
```

### n8n Integration

- **Single upload (with social post):**
  - Webhook: `https://n8n.ontext.info/webhook/sticker-uploaded`
  - Workflow: "SH webhook poster" (ID: `dGmModNhxarQFxjf`) — schedules social post (Zernio) + one `repository_dispatch` per sticker.
- **Batch upload (no social post):**
  - Webhook: `https://n8n.ontext.info/webhook/sticker-batch-uploaded`
  - Workflow: "SH batch reconcile" (ID: `kpeWoT8qqyq0Gdrq`) — thin trigger: one `repository_dispatch` for the WHOLE batch (`sticker_ids` comma-list + `notify:true` + counts). No social post.
- **Clubs poller:** "SH clubs poller (change detector)" (ID: `qESondLX2tc7dMmH`) — runs every minute, detects new/changed `clubs` rows and dispatches club/country page regeneration. This is what picks up `club-create` inserts and Re-enrich updates.
- **GitHub credential:** `githubApi` (n8n credential ID: `ivaldofdLM73JGXZ`)

### GitHub Actions

- **Workflow:** `.github/workflows/generate-sticker-pages.yml`
- **Concurrency:** serialized, cancel-in-progress: false, only ONE pending run queued.
- **Multi-ID runs:** accepts `client_payload.sticker_ids` as a comma list and loops `generate-single-sticker.js` over all of them in ONE run — this is how a whole batch is generated without concurrency-group cancellations.
- **Telegram notify (batch only):** final step fires only when `client_payload.notify == 'true'` — sends a summary to Victor (Самаритянин bot, chat 292048) on success/failure. Single uploads stay silent. Needs GitHub secret `TELEGRAM_BOT_TOKEN`.
- **Self-healing:** each run regenerates sticker N-1's page (navigation links).
- **Push retry:** git stash -> git pull --rebase -> git stash pop (up to 3 attempts).
- **Belt-and-suspenders:** `reconcile-stickers.yml` (cron */15 min) generates any sticker page that has no on-disk HTML, regardless of how it was uploaded.

## Hybrid Architecture

| Type | Pages | Purpose |
|------|-------|---------|
| Static (pre-rendered) | stickers/, clubs/, countries/, cities/ | SEO |
| Dynamic (client-side) | quiz, profile, leaderboard, battle, map, catalogue | Interactivity |

## Game Modes

| Mode | Timer | Description |
|------|-------|-------------|
| Classic | 10 sec/question | Guess club, earn points, combo streaks |
| Time To Run | 60 sec total | As many as you can in a minute |
| Daily Quiz | 45 sec start | 18 stickers, daily challenge for everyone |

## Battle Mode

ELO rating system for sticker comparison. Algorithm uses exponential weighting for fair distribution:
- New stickers (games=0) get 40% forced selection chance
- Recent stickers get 80% weight reduction (soft exclusion)
- Three modes: Close rating (40%), Random (40%), Extreme matchups (20%)

## Environment

```
scripts/.env:
  SUPABASE_URL
  SUPABASE_ANON_KEY
  SUPABASE_SERVICE_KEY (for image optimization)
  ANTHROPIC_API_KEY (for AI enrichment)

GitHub Secrets:
  SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_KEY
  TELEGRAM_BOT_TOKEN (Самаритянин bot — batch generation notify)

Vercel Environment Variables:
  OPENAI_API_KEY (api/enrich-club.js — club enrichment)
```

## Key Rules

1. `templates/index-page.html` is SOURCE OF TRUTH for homepage. Always edit template, not index.html.
2. Gaps in sticker IDs are normal (deleted from DB).
3. 302 redirects (vercel.json) are temporary, change to 301 after stabilization.
4. Service Key != anon key. Keep secret.
5. Image optimization takes 30-60 min for all stickers.
6. When changing SEO in templates/generators, update ALL generator scripts consistently.

## File Structure

```
stickerhunt/
+-- upload.html / upload.js          # Single sticker uploader (with social post)
+-- upload-batch.html / .js          # Batch uploader (no social post; whole page = drop zone)
+-- club-create.html / .js           # Create club + AI enrichment + Re-enrich
+-- api/
|   +-- enrich-club.js               # Vercel fn: club city/hashtags/website via OpenAI
|   +-- img-proxy.js                 # Image proxy
+-- stickers/          # Static sticker HTML pages
+-- clubs/             # Static club HTML pages
+-- countries/         # Static country HTML pages
+-- cities/            # Static city HTML pages + index
+-- templates/         # 5 HTML templates
+-- scripts/
|   +-- seo-helpers.js              # Shared SEO module
|   +-- generate-single-sticker.js  # Main generator (GitHub Actions)
|   +-- regenerate-*.js             # Batch regeneration scripts
|   +-- generate-city-pages.js      # City page generator
|   +-- generate-static-pages.js    # Full local regeneration
|   +-- optimize-images.js          # Image WebP converter
|   +-- test-generators.js          # Test suite (53 tests)
|   +-- wiki-cache.json             # Club descriptions cache
|   +-- city-wiki-cache.json        # City data cache
+-- docs/              # This documentation
+-- sitemap*.xml       # Sitemaps
+-- .github/workflows/ # CI/CD
+-- vercel.json        # Redirects config
```
